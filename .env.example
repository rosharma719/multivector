# Optional Hugging Face read token. It removes anonymous-download rate limits.
# Create one at https://huggingface.co/settings/tokens, then copy this file to
# .env and replace the value. Never commit .env.
HF_TOKEN=
